Run security audit:
1. innerHTML without esc() on user data
2. eval/Function/setTimeout-string usage
3. API endpoints without rate limits
4. Worker handlers without input validation
5. CSP meta tag presence
6. HSTS header in _headers
7. Secrets leaked in code (API keys, tokens)
8. File type blocking (BLOCKED_EXTENSIONS)
Report findings with line numbers.
